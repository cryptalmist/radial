name: Fabric 26.1.2 Backport Build
on:
  push:
    branches: [ "fabric-26.1.2" ]
  pull_request:
    branches: [ "fabric-26.1.2" ]
  workflow_dispatch:

jobs:
  build:
    name: Build 26.1.2 Fabric Jar (GitHub-only, no local build)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Validate Gradle Wrapper
        uses: gradle/actions/wrapper-validation@v6

      - name: Setup Java 25 (MC 26.1.2)
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '25'

      - name: Cache Gradle Packages
        uses: actions/cache@v5
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-26.1.2-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}

      - name: Make Gradle Executable
        run: chmod +x gradlew

      - name: Build JAR
        run: ./gradlew build --stacktrace --no-daemon

      - name: Clean Extra Artifacts
        run: |
          rm build/libs/*-sources.jar build/libs/*-dev.jar || true

      - name: Upload Artifact
        uses: actions/upload-artifact@v7
        with:
          name: radial-fabric-26.1.2-${{ github.sha }}
          path: build/libs/*.jar
          if-no-files-found: error
